class IconObjects {
  constructor() {
  }

  static #makeIconDiv(target) {
    const fontSize = 8;
    const iconDiv = $('<div>');
    return iconDiv;
  }

  static #getPtValueFromStylesheets(selector) {
    let result;
    return result;
  }

  static showIcon(iconSetting) {

    const iconDiv = this.#makeIconDiv(IconObjects.iconList[icon]);

    const inputAreaCSS = this.#getPtValueFromStylesheets(`#${id}`);

  }
}
